diff --git a/examples/companion_radio/main.cpp b/examples/companion_radio/main.cpp
index 876dc9c..8d8bdb4 100644
--- a/examples/companion_radio/main.cpp
+++ b/examples/companion_radio/main.cpp
@@ -2,6 +2,13 @@
 #include <Mesh.h>
 #include "MyMesh.h"
 
+#ifdef PIN_BUZZER
+#include <NonBlockingRtttl.h>
+
+// Imperial March (Star Wars) — plays at startup
+const char IMPERIAL_MARCH[] = "starwars:d=4,o=5,b=250:g,g,g,2d#,8a#,g,2d#,g,4d#,d,c,2a#,g,d,c,a#,g#,g,2f";
+#endif
+
 // Believe it or not, this std C function is busted on some platforms!
 static uint32_t _atoi(const char* sp) {
   uint32_t n = 0;
@@ -123,6 +130,15 @@ void setup() {
   }
 #endif
 
+#ifdef PIN_BUZZER
+  // Startup jingle: Imperial March (Star Wars) ~5 seconds
+  rtttl::begin(PIN_BUZZER, IMPERIAL_MARCH);
+  while (!rtttl::done()) {
+    rtttl::play();
+    delay(5);
+  }
+#endif
+
   if (!radio_init()) { halt(); }
 
   fast_rng.begin(radio_get_rng_seed());
